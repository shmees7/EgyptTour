<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musically">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".eminem_notafraid"
            android:label="@string/eminem"/>
        <activity android:name=".justen_loveyourself"
            android:label="@string/justen"/>
        <activity android:name=".shakera_lalala"
            android:label="@string/shakera"/>
        <activity android:name=".maherzain_medina"
            android:label="@string/maher"/>
    </application>

</manifest>